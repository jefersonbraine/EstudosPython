# import math
 
# from math import sqrt, sin #*importar coisas especificas

# from math import * #* import universal sem precisar ficar digitando o nome toda vez

# print(math.sqrt(81))
# print(sqrt(81))

# import math as m #* mudar nome do import (apelido/alias)

# print(m.sqrt(92))

# import mod_func as mf #*importando modulos meus

# if __name__ == '__main__': #*instrução condicional para controlar o fluxo do código, 
    #* ela verifica se uma variável especial interna do python chamada name é igual a esse valor main, se for o bloco de código é executado, se não é ignorado

    #* name é uma variável que tem o nome do módulo atual, que está rodando, quando o módulo é executado como se fosse o programa principal, o valor de name é definido como main, quando o módulo é importado o name é definido como o nome do módulo, isso é para evitar que partes do código sejao executadas quando importamos o módulo.

#     mf.mensagem()
#     num = int(input('Digite um número inteiro:'))

#     print(f'\nCalcular fatorial do número: ')
#     fat = mf.fatorial(num)
#     print(f'O fatorial é {fat}')

#     print(f'\nCalcular sequência de fibonacci do número: ')
#     fib = mf.fibo(num)
#     print(f'O fatorial é {fib}')


